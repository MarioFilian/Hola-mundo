name: static-site
services:
  - name: web
    environment_slug: html
    github:
      repo: mariofilian/Hola-mundo  # <--- Usa aquí tu repo real
      branch: main
      deploy_on_push: true
    source_dir: /
    routes:
      - path: /

